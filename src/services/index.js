const horarios_db = require("./horarios");
const reservaciones_db = require("./reservaciones");
const actividades_db = require("./actividades");
const trabajadores_db = require("./trabajadores");
const tours_db = require("./tours");

module.exports = {
  horarios_db,
  reservaciones_db,
  actividades_db,
  trabajadores_db,
  tours_db,
};
